<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Pool Game Tracker</title>
  <link rel="stylesheet" href="style.css"/>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script src="https://maps.googleapis.com/maps/api/js?key=YOUR_API_KEY&libraries=places"></script>
</head>
<body>
  <div class="container">
    <h1>🎱 Pool Game Tracker</h1>

    <div id="auth">
      <div id="register">
        <h2>Register</h2>
        <input id="reg-username" placeholder="Username" required/>
        <input id="reg-email" type="email" placeholder="Email" required/>
        <input id="reg-password" type="password" placeholder="Password" required/>
        <input id="reg-security" placeholder="Your favorite cue color?" required/>
        <button onclick="register()">Register</button>
        <p><a onclick="toggleAuth('login')">Already have an account?</a></p>
      </div>

      <div id="login" style="display:none;">
        <h2>Login</h2>
        <input id="login-email" type="email" placeholder="Email" required/>
        <input id="login-password" type="password" placeholder="Password" required/>
        <button onclick="login()">Login</button>
        <p><a onclick="toggleAuth('register')">Create Account</a></p>
        <p><a onclick="showRecovery()">Forgot Login Info?</a></p>
      </div>

      <div id="recovery" style="display:none;">
        <h2>Recover Account</h2>
        <input id="recover-email" type="email" placeholder="Registered Email"/>
        <input id="recover-security" placeholder="Favorite cue color?"/>
        <button onclick="recover()">Recover</button>
        <p><a onclick="toggleAuth('login')">Back to Login</a></p>
      </div>
    </div>

    <div id="app" style="display:none;">
      <nav class="nav">
        <button data-view="dashboard">Dashboard</button>
        <button data-view="record">Record Game</button>
        <button data-view="stats">Statistics</button>
        <button data-view="map">Find Pool Halls</button>
        <button onclick="logout()">Logout</button>
      </nav>

      <section id="dashboard-view" class="view">
        <h2>Dashboard</h2>
        <p id="user-welcome"></p>
        <button id="reset-data">Reset All Records</button>
        <button id="load-data">Reload Data</button>
        <ul id="game-list"></ul>
      </section>

      <section id="record-view" class="view" style="display:none;">
        <h2>Record Game</h2>
        <form id="game-form">
          <input id="opponent" placeholder="Opponent Name" required />
          <input id="gameType" placeholder="Game Type" required />
          <input id="raceTo" type="number" placeholder="Race To" required />
          <input id="dateTime" type="datetime-local" required />
          <select id="won" required>
            <option value="">Did you win?</option>
            <option value="true">Yes</option>
            <option value="false">No</option>
          </select>
          <button type="submit">Add Game</button>
        </form>
      </section>

      <section id="stats-view" class="view" style="display:none;">
        <h2>Statistics</h2>
        <canvas id="statsChart"></canvas>
        <canvas id="gamesChart"></canvas>
      </section>

      <section id="map-view" class="view" style="display:none;">
        <h2>Nearby Pool Halls</h2>
        <input id="map-location" placeholder="Enter your city or ZIP" />
        <button onclick="initMap()">Find</button>
        <div id="map" style="height: 400px; margin-top: 1rem;"></div>
      </section>
    </div>
  </div>
  <script src="script.js"></script>
</body>
</html>
